# Generated by scripts/fetch-news.mjs and published straight into the Pages
# artifact by the workflow — never committed, so it can never cause a merge
# conflict between your local edits and the scheduled refresh.
data/auto-news.json

# Local ingest log
ingest.log

# OS / editor noise
.DS_Store
Thumbs.db
*.swp
